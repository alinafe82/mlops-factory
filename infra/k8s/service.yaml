apiVersion: v1
kind: Service
metadata:
  name: inference
  namespace: mlops-factory
  labels:
    app: inference
spec:
  selector:
    app: inference
  ports:
  - name: http
    port: 80
    targetPort: 8080
  type: ClusterIP
